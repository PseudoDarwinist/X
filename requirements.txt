# Resume Journey Map Generator - Dependencies

# Core Python version: 3.6+

# PDF parsing support
pdfplumber>=0.9.0

# DOCX parsing support
python-docx>=0.8.11

# NLP-based extraction (optional but recommended)
spacy>=3.5.0

# Web interface
Flask>=2.3.0
Werkzeug>=2.3.0

# Additional dependencies
Pillow>=9.5.0  # Image processing for PDF and PNG export

# Development dependencies (optional)
pytest>=7.3.0  # For testing
black>=23.3.0  # Code formatting
flake8>=6.0.0  # Linting

# Installation instructions:
#
# Basic installation (CLI only, TXT files):
#   pip install -r requirements.txt --only-binary :all:
#
# Full installation with all features:
#   pip install -r requirements.txt
#   python -m spacy download en_core_web_sm
#
# Minimal installation (no PDF/DOCX support):
#   pip install Flask Werkzeug
#
# For NLP features, after installing spacy:
#   python -m spacy download en_core_web_sm
